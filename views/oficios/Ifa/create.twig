{% extends 'layoutForm.twig' %}
{% block title %}
  <div class="header-irac">
    <a  href="#!" class="active">Nueva Asignacion</a>
    <a href="/SIA/juridico/Ifa/historial/{{ id }}" class="no-active">Respuestas</a>
  </div>
{% endblock %}

{% block form %}

    <form id="asignacion"   data-ruta="Ifa" method="POST" enctype="multipart/form-data">
      <input type="hidden" value="{{ id }}" id="idVolante" name="idVolante">
      <input type="hidden" name="idEstadoTurnado" value='NUEVO'>
      <div class="row row-form">
        
       <div class="col-lg-5">
        <label>Enviar A: </label>
        <select name="idUsrReceptor" class="form-control" required>
          <option value="">Seleccione una Opcion</option>
          {% for persona in personas %}
            <option value="{{ persona.idPuestoJuridico }}">
              {{ persona.saludo }} {{ persona.nombre }} {{ persona.paterno }} {{ persona.materno }}
            </option>
          {% endfor %}
        </select>
      </div>

      <div class="col-lg-3">
        <label>Prioridad</label>
        <select class="form-control" name="idTipoPrioridad" required>
          <option value="">Seleccione una Opcion</option>
          <option value="NORMAL">Normal</option>
          <option value="URGENTE">Urgente</option>
        </select>
      </div>
      
      </div>

      <div class="row row-form">
        <div class="col-lg-11">
          <label>Anexar Documento</label>
          <input type="file" name="file" class="form-control">
        </div>
      </div>

      <div class="row row-form">
        <div class="col-lg-11">
            <label>Comentario</label>
            <textarea placeholder="Comentario" class="form-control" name="comentario"></textarea>
        </div>
      </div>
       <div class="form-group send">
            <input class="btn btn-primary btn-sm" type="submit" value="Guardar"/>
            <button class="btn btn-danger btn-sm" id="cancelar" data-ruta="Ifa">Cancelar</button>
      </div>

    </form>
   {% include 'errors.twig' %}
{% endblock %}
